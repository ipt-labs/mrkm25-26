
# Лабораторна робота №3-4: Реалізація Web-сервісу електронного цифрового підпису

**Дисципліна:** Методи реалізації криптографічних механізмів (МРКМ)  
**Університет:** КПІ ім. Ігоря Сікорського, Фізико-технічний інститут  
**Група:** ФБ-41мн  
**Виконавці:** Зубко Д. Е., Тивонюк В. І.  
**Дата:** 01.11.2025


## 📋 Мета та завдання роботи

**Мета:** Дослідити можливості побудови криптографічних протоколів за допомогою асиметричних криптосистем та отримати практичні навички у створенні гібридних криптосистем на прикладі веб-сервісу електронного цифрового підпису (ЕЦП).

**Ключові завдання:**
1.  Розробити децентралізований додаток (DApp) для реєстрації та верифікації цифрового контенту.
2.  Створити смарт-контракт на мові **Solidity** для зберігання доказів існування контенту (хешів) та цифрових підписів у блокчейні **Ethereum**.
3.  Налаштувати локальне середовище розробки за допомогою **Brownie** та локального блокчейну **Ganache**.
4.  Розробити веб-інтерфейс на **Python (Flask)** та **HTML/JS** для взаємодії користувачів зі смарт-контрактом.
5.  Інтегрувати браузерний гаманець **MetaMask** для безпечного створення цифрових підписів на стороні клієнта.
6.  Продемонструвати контрольний приклад роботи системи та неможливість атаки побічним каналом на сервер.

---

## 🔧 Інструменти та архітектура системи

Розроблена система є повноцінним DApp і складається з трьох основних компонентів:

1.  **Бекенд (Smart Contract):**
    *   **Мова:** Solidity v0.8.19
    *   **Блокчейн:** Локальна мережа Ethereum, емульована за допомогою **Ganache**.
    *   **Функціонал:** Смарт-контракт `ContentRegistry` дозволяє реєструвати унікальний хеш файлу, його цифровий підпис та адресу власника, забезпечуючи незмінність та публічну доступність цих даних.

2.  **Серверна частина (Web Server):**
    *   **Фреймворк:** Python (Flask)
    *   **Бібліотека для взаємодії з блокчейном:** Web3.py
    *   **Функціонал:** Сервер виступає як міст між фронтендом та смарт-контрактом, обробляє запити від користувача, викликає функції контракту та повертає результати.

3.  **Клієнтська частина (Frontend):**
    *   **Технології:** HTML, CSS, JavaScript
    *   **Бібліотека для взаємодії з гаманцем:** Ethers.js
    *   **Гаманець:** **MetaMask** використовується як безпечний менеджер ключів для підписання повідомлень (хешів файлів) на стороні клієнта.

---

## 🚀 Хід виконання роботи

### Крок 1: Налаштування середовища та створення контракту
-   Ініціалізовано проєкт за допомогою фреймворку **Brownie**: `brownie init`.
-   Налаштовано локальний блокчейн **Ganache** для тестування.
-   Розроблено смарт-контракт `ContentRegistry.sol`, який містить:
    -   Структуру `ContentInfo` для зберігання адреси власника, часової мітки та підпису.
    -   Функцію `registerContent(bytes32 _contentHash, bytes memory _signature)` для реєстрації нового контенту з перевіркою підпису.
    -   Допоміжну функцію `verifySignature` для верифікації підпису ECDSA за допомогою `ecrecover`.
-   Контракт успішно скомпільовано командою `brownie compile --all`.

### Крок 2: Розгортання (Deploy) контракту
-   Створено Python-скрипт (`scripts/deploy_web3.py`) для розгортання скомпільованого контракту в мережі Ganache.
-   Скрипт успішно виконав деплой, а адреса нового контракту була автоматично записана у файл `.env` для використання веб-сервером.


### Крок 3: Налаштування MetaMask та веб-додатку
-   Створено та налаштовано акаунт у **MetaMask**.
-   Додано локальну мережу Ganache (RPC URL: `http://127.0.0.1:7545`, Chain ID: `1337`).
-   Імпортовано один з акаунтів з Ganache у MetaMask для отримання тестового ETH.
-   Запущено веб-сервер на Flask командою `python app.py`, який почав обслуговувати клієнтський інтерфейс.

### Крок 4: Демонстрація роботи системи
-   **Підключення гаманця:** Користувач підключає свій MetaMask до веб-додатку.
-   **Створення підпису:** Користувач обирає файл. JavaScript на клієнті обчислює його SHA-256 хеш. MetaMask пропонує користувачу **підписати** цей хеш за допомогою його **приватного ключа**.
-   **Реєстрація:** Підписаний хеш, сам підпис та адреса користувача відправляються на сервер, який викликає функцію `registerContent` смарт-контракту. Транзакція записується в блокчейн.
-   **Верифікація:** Будь-який користувач може обрати той самий файл (або вставити його хеш) і перевірити, ким і коли він був зареєстрований. Система повертає адресу власника та час реєстрації.


### Крок 5: Демонстрація логіки безпеки
-   **Спроба повторної реєстрації:** При спробі зареєструвати той самий файл ще раз (навіть з іншого акаунта) смарт-контракт коректно відхилив транзакцію з помилкою `Content hash already registered`, що підтверджує неможливість дублювання записів.


---

## 🛡️ Аналіз безпеки: Неможливість атаки побічним каналом

У розробленій архітектурі атака побічним каналом на сервер з метою викрадення приватного ключа **є неможливою** з фундаментальної причини: **ізоляції приватного ключа**.

1.  **Клієнтське підписання:** Критична операція — створення цифрового підпису — відбувається виключно **на стороні клієнта** у захищеному середовищі гаманця MetaMask.
2.  **Відсутність доступу у сервера:** Наш веб-сервер на Flask **ніколи не отримує, не зберігає і не обробляє** приватний ключ користувача. Сервер оперує лише публічними даними: хешем, готовим підписом та публічною адресою.
3.  **Перенесення відповідальності:** Захист від атак побічними каналами (аналіз часу виконання, енергоспоживання тощо) покладається на розробників MetaMask, чиє ПЗ спеціально спроєктоване для безпечного зберігання криптографічних ключів.

Таким чином, архітектура DApp за своєю природою є стійкою до цього вектору атак.

---

## 🔍 Висновки

У ході лабораторної роботи було успішно розроблено та досліджено повнофункціональний децентралізований веб-сервіс ЕЦП на базі блокчейну Ethereum. Було продемонстровано повний життєвий цикл системи: від підключення гаманця та створення підпису до його успішної реєстрації в блокчейні та подальшої публічної верифікації.

Підтверджено коректну роботу логіки безпеки, зокрема неможливість повторної реєстрації контенту. Також було теоретично обґрунтовано та продемонстровано архітектурну стійкість системи до атак побічними каналами на сервері завдяки клієнтській ізоляції приватних ключів.
